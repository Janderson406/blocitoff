<li class="list-group-item">
  <span class="badge">
    <%= item.created_at.strftime("%m. %d. %Y") %>
  </span>
    <%= link_to "", [@user, item], method: :delete, class: 'glyphicon glyphicon-ok' %> &nbsp; <%= item.name %>

</li>
