
<h4>Add an Item</h4>

 <%= form_for [@user, @item] do |f| %>
   <div class="form-group col-xs-7">
     <%= f.text_field :name, class: 'form-control', placeholder: "Enter a new to-do" %>
   </div>
   <%= f.submit "Submit", class: 'btn btn-default' %>
 <% end %>
