<div class="well well-lg">
  <h3><%= current_user.username %>'s To-Do List</h3> </br>
</div>

  <%= render partial: "/items/form", locals: { user: @user, item: @item } %> </br>
  <%= render @items %>
  </br>
    <div class="panel panel-footer">
      <%= current_user.email %> </br>
      active since: <%= current_user.created_at.strftime("%m-%d-%Y") %>
    </div>
